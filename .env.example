APP_ENV=development
APP_HOST=127.0.0.1
APP_PORT=8000

# LLM keys are entered through the local UI and stored in Windows Credential Manager.
# GigaChat endpoints and its verified trust anchor are bundled by the application.
